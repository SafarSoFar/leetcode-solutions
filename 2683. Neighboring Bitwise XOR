class Solution {
public:
    bool doesValidArrayExist(vector<int>& derived) {
        int ones = 0;
        for(auto& el : derived){
            if(el == 1)
                ones++;
        }
        return ones % 2 == 0;
    }
};
